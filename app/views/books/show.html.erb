<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @book.user.name %>
</p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', user_books_path(@book.user_id) %>
